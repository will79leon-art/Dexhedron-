# Dexhedron-

## Backtrace integration

A simple backtrace configuration file (backtrace.yml) has been added with `backtrace: true`.
Keep this file at the repository root to enable basic backtrace support.
For language-specific or runtime integrations, add the appropriate dependency or configuration for your project (for example, enable stack traces in your runtime or add an error-reporting/backtrace SDK).
